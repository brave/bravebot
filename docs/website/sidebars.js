// @ts-check

/**
 * The sidebar is generated from the docs folder structure: each directory is a
 * category described by its `_category_.json`, and each page's position comes
 * from its own front matter.
 */

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  docsSidebar: [{type: 'autogenerated', dirName: '.'}],
};

module.exports = sidebars;
